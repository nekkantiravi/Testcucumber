package com.macys.sdt.projects.Marketing.StoresLocator.utils;

import org.openqa.selenium.WebElement;

/*
 * This class represents the store buildings data
 * It also contains informationj about departments in the store
 */
public class StoreBuildings {
  //TODO: Determine how (if?) to structure this...
  // Each location has one or more buildings
  // Each building has multiple elements
  // Each element has floor/category/department items
  // Each item has id/name/(optional)seqNumber
}
