{
    "include": [
        "panel.pagination_top"
    ],
    
    "product_count_span":               "className, productCount",
    "product_count_h3":                 "id, weFoundMsg",
    "loading_mask":                     "id, loading",
    "chanel_prod_container":            "id, thumbnails",
    "product_count_breadcrumb":         "className, productCount",
    "selected_facets_container":        "className, facetItemContainer",

    "sort_by":                          "id, sortBy",
    "sort_by_options":                  "className, sortByItens",
    "sort_by_container":                "id, filterSort",
    "sort_by_text":                     "linkText, SORT BY:",
    "top_pagination":                   "className,paginationTop||className, newPagination",
    "bottom_pagination":                "className,paginationBottom",
    "pages":                            "xpath, //div[contains(@class,'paginationTop')] || className,newPagination",
    "top_pagination_dropdown":          "xpath, //div[contains(@class,'paginationTop')]//div[contains(@class,'dropdown')] || id, paginateByTop",
    "top_pagination_section":           "xpath, //div[contains(@class,'paginationTop')]//div[@class='custom dropdown']//li || xpath, //button[@id='paginateByTop']/following-sibling::ul/li",
    "bottom_pagination_dropdown":       "xpath, //div[contains(@class,'paginationBottom')]//div[contains(@class,'dropdown')] || id, paginateByBottom",
    "bottom_pagination_section":        "xpath, //div[contains(@class,'paginationBottom')]//div[@class='custom dropdown']//li || xpath,//button[@id='paginateByBottom']/following-sibling::ul/li",
    "selected_tab":                     "id, allProducts",
    "tab_veiw":                         "id, facetsGrayBar",
    "unselected_tab":                   "id, localStore || id, UPC_BOPS_PURCHASABLE",
    "selected_items_per_page":          "xpath, //div[@id='filterPPP']/a[@class='selected']",
    "search_message":                   "xpath, //div[@class='searchKeyword bgvSearchKeywordLeft']"
}
